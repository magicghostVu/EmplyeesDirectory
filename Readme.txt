1. Import file emdir vào cơ sở dữ liệu, vào một csdl mới, tên tùy ý. 
2. mở project lên, trong thư mục src có file hibernate.cfg.xml, mở file này lên và chỉnh sửa như sau: 
	a. dòng 7, ông phải sửa lại cái tên csdl cho đúng với tên đã tạo trong bước 1
	b. dòng 8 và dòng 9 thì sửa user name với pass cho phù hợp với máy. 